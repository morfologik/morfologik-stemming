- remove all use* methods from DictionaryMetadata, replace with encoder name.
- provide backcompat for boolean flags (dictionary metadata reading only)?
- cleanup DictionaryLookup.decodeBaseForm -> this should be really inlined where it's actually
 used and use the proper encoder.decode method.

